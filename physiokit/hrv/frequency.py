import numpy.typing as npt


def compute_hrv_frequency(rr_intervals: npt.NDArray, sample_rate: float = 1000, axis: int = -1):
    """Compute the frequency domain HRV features."""
    raise NotImplementedError()
